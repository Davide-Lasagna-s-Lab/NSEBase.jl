# API reference

The reference is divided by subsystem. The case manual is the recommended
entry point for choosing constructors; these pages record exact signatures,
fields, invariants, and return values.

```@index
Pages = ["api/grids.md", "api/fields.md", "api/operations.md", "api/equations.md", "api/cases.md"]
```
